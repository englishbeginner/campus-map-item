spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_map?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379

amap:
  api-key: your_amap_api_key_dev